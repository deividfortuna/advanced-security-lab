# Dev-only image — exercises the `Dockerfile.<suffix>` file-name pattern.

FROM python:3.12-slim
WORKDIR /app
RUN pip install watchdog
COPY . .
CMD ["python", "-m", "watchdog.watchmedo", "auto-restart", "--", "python", "app.py"]
